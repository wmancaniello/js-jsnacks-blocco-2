/**
 * Descrizione: generare userNumber(n) array, ognuno formato da 10 numeri casuali tra 1 e 100
 * @param {number} numero
 * @returns {any}
 */
function generateRndArray(numero) {
    for (let i = 0; i < numero; i++) {
        const array = [];
    }

}